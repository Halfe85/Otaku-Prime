Otaku Prime 0.1.2 Alpha9
========================

- Replace provider-separated watchlist rows with one canonical Prime watchlist item per verified anime identity.
- Add AniList, MyAnimeList, Kitsu, and Simkl ID columns to every canonical item.
- Keep provider snapshots as connector state rather than separate user-facing watchlists.
- Merge provider entries only through verified overlapping IDs; never merge by title guessing.
- Preserve each provider's original status, normalized status, progress, update time, and raw response.
- Normalize watching, completed, paused/on-hold, dropped, and planning states across all four trackers.
- Preserve AniList REPEATING and normalize it to Prime's watching state.
- Make Prime's canonical status and progress authoritative after the initial merge.
- Detect provider disagreements without overwriting Prime's master state.
- Fetch AniList's native MAL mapping and Kitsu's AniList/MAL mappings.
- Retain every AniList/MAL/Kitsu/Simkl ID returned by Simkl watchlist records.
- Resolve missing AniList, MAL, Kitsu, and Simkl catalog IDs for every Prime watchlist item, regardless of which tracker accounts are connected.
- Reject and record Simkl parent-title fallbacks when their IDs disagree with the authoritative watchlist item, instead of overwriting or repeatedly retrying them.
- Retry Simkl parent-title redirects through exact external-ID search and accept only detail records that agree with every authoritative ID Prime already knows.
- Show catalog-identity conflicts separately from watch-progress conflicts in the series-details modal.
- Use Simkl's current anime watchlist endpoint and report empty or malformed API responses cleanly.
- Refresh expired MAL and Kitsu access tokens before fetching their lists.
- Use Simkl activities and delta synchronization during periodic runs instead of skipping Simkl.
- Update Watchlist Management to show one row with all known IDs, Prime state, and connected sources.
- Open each watchlist row in a responsive series-details modal with canonical state, titles, conflict status, and direct AniList, MAL, Kitsu, and Simkl links.
- Migrate Alpha8b provider rows into the Alpha9 canonical schema without losing fetched entries.
